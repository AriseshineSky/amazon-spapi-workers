# Limit systemd journal size (Celery worker logs via journalctl).
# Installed to /etc/systemd/journald.conf.d/amazon-spapi-size.conf

[Journal]
SystemMaxUse=512M
SystemKeepFree=1G
MaxRetentionSec=14day
